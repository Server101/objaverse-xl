version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install --upgrade pip
      - pip install -r requirements.txt
      - pip install -e .
run:
  runtime-version: 3.11
  command: uvicorn objaverse.api:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
